# 学習進捗動画記録機能ガイド

## 概要

この機能は、学習中のロボットの挙動を定期的に動画で記録し、学習進捗を可視化するための機能です。学習開始時から定期的に動画を保存し、複数のエピソードを同一動画にまとめることができます。

## 機能の特徴

### 1. 定期的な動画保存
- 学習開始時から設定された頻度で動画を自動保存
- デフォルト: 5分ごと（180,000ステップごと）
- 学習進捗の確認に最適

### 2. 複数エピソードの統合
- 1つの動画に複数のエピソードをまとめて記録
- デフォルト: 3エピソードを1つの動画に統合
- 偶然性を避けつつ、モデルの挙動を確認可能

### 3. 学習情報のオーバーレイ
- エピソード番号、学習ステップ数、報酬値、距離、成功フラグを表示
- リアルタイムで学習状況を確認可能
- カスタマイズ可能な表示設定

### 4. 高品質な動画出力
- 解像度: 640x480、60fps（滑らかな動画）
- 形式: MP4（H.264）
- シミュレーション240Hzから動画60fpsへの適切な変換
- ファイルサイズを考慮した最適化

## 設定方法

### 1. 設定ファイルの編集

`config/training_config_12h_improved.yaml`に以下の設定を追加：

```yaml
# 学習進捗動画保存設定
video_recording:
  enabled: true                   # 動画保存を有効化
  frequency: 180000              # 約5分ごとに動画保存（学習進捗確認用）
  episodes_per_video: 3          # 1つの動画に含めるエピソード数
  max_steps_per_episode: 1200    # エピソードあたりの最大ステップ数（5秒分）
  video_quality:
    fps: 60                      # フレームレート（滑らかな動画）
    resolution: [640, 480]       # 解像度 [width, height]
    format: "mp4"                # 動画形式
  overlay_info: true             # 学習情報のオーバーレイ表示
  info_font_size: 16             # フォントサイズ
  info_color: [255, 255, 255]    # テキスト色 [R, G, B]
  info_position: [10, 10]        # テキスト位置 [x, y]
```

### 2. 設定パラメータの説明

| パラメータ | 説明 | デフォルト値 |
|-----------|------|-------------|
| `enabled` | 動画記録の有効/無効 | `true` |
| `frequency` | 記録頻度（ステップ数） | `180000` |
| `episodes_per_video` | 1動画あたりのエピソード数 | `3` |
| `max_steps_per_episode` | エピソードあたりの最大ステップ数 | `1200` |
| `fps` | フレームレート | `60` |
| `resolution` | 解像度 [幅, 高さ] | `[640, 480]` |
| `format` | 動画形式 | `"mp4"` |
| `overlay_info` | 情報オーバーレイの有効/無効 | `true` |
| `info_font_size` | フォントサイズ | `16` |
| `info_color` | テキスト色 [R, G, B] | `[255, 255, 255]` |
| `info_position` | テキスト位置 [x, y] | `[10, 10]` |

## 使用方法

### 1. 通常の学習実行

動画記録機能は既存の学習プロセスに統合されているため、特別な操作は不要です：

```bash
# 学習の実行（動画記録機能付き）
python scripts/train.py --config config/training_config_12h_improved.yaml
```

### 2. 動画記録のテスト

動画記録機能をテストする場合：

```bash
# 動画記録機能のテスト
python scripts/test_video_recording.py --test-type both
```

### 3. 動画ファイルの確認

動画ファイルは以下のディレクトリに保存されます：

```
data/experiments/[実験名]/training_progress_step[ステップ数]_ep[エピソード数]_[タイムスタンプ].mp4
```

例：
```
data/experiments/12h_training_20250101_120000/training_progress_step180000_ep1_20250101_120500.mp4
```

## 動画の内容

### 1. 表示される情報

各フレームに以下の情報がオーバーレイ表示されます：

- **Episode**: エピソード番号
- **Step**: 現在のステップ数
- **Reward**: 現在の報酬値
- **Distance**: 前進距離（メートル）
- **Success**: 成功フラグ（True/False）
- **Time**: 記録時刻

### 2. エピソード区切り

各エピソードの完了時には、黒い区切りフレームが1秒間表示され、エピソード完了情報が表示されます。

### 3. 動画の構成

1つの動画ファイルには以下の内容が含まれます：

1. エピソード1の実行（最大5秒）
2. エピソード1完了の区切りフレーム
3. エピソード2の実行（最大5秒）
4. エピソード2完了の区切りフレーム
5. エピソード3の実行（最大5秒）
6. エピソード3完了の区切りフレーム

## パフォーマンスへの影響

### 1. 学習速度への影響

- 動画記録は並列環境の最初の環境（env_id=0）のみを記録
- 記録頻度は設定可能（デフォルト: 5分ごと）
- 学習速度への影響は最小限

### 2. ストレージ使用量

- 1つの動画ファイル: 約20-100MB（5秒×3エピソード、60fps）
- 12時間の学習: 約144個の動画ファイル
- 総容量: 約2.9-14.4GB

### 3. メモリ使用量

- 動画記録用の追加メモリ: 約50-100MB
- フレームバッファ: 約10-20MB

## トラブルシューティング

### 1. 動画が作成されない場合

**原因**: 並列環境でのレンダリングが無効になっている

**解決方法**:
```python
# parallel_env.pyでrenderメソッドが実装されていることを確認
# または、ダミーフレームが生成されることを確認
```

### 2. 動画ファイルが破損している場合

**原因**: 動画記録の途中でプロセスが終了した

**解決方法**:
- 学習プロセスを正常に終了する
- 動画記録のクリーンアップが実行されることを確認

### 3. オーバーレイ情報が表示されない場合

**原因**: フォント設定の問題

**解決方法**:
```yaml
# configファイルでフォントサイズを調整
info_font_size: 20  # より大きなフォントサイズ
```

### 4. 動画の品質が低い場合

**原因**: 解像度やフレームレートの設定

**解決方法**:
```yaml
video_quality:
  fps: 60                    # より高いフレームレート
  resolution: [1280, 720]   # より高い解像度
```

## カスタマイズ

### 1. 記録頻度の調整

```yaml
video_recording:
  frequency: 90000  # より頻繁に記録（約3分ごと）
```

### 2. エピソード数の調整

```yaml
video_recording:
  episodes_per_video: 5  # より多くのエピソードを1つの動画に
```

### 3. 動画品質の調整

```yaml
video_quality:
  fps: 60                      # 60fps（滑らかな動画）
  resolution: [1280, 720]      # より高い解像度
  format: "mp4"
```

**注意**: フレームレートを変更する場合、シミュレーション周波数（240Hz）との比率が適切になるようにしてください：
- 30fps: 8:1の比率（8フレームごとに1フレーム記録）
- 60fps: 4:1の比率（4フレームごとに1フレーム記録）
- 120fps: 2:1の比率（2フレームごとに1フレーム記録）

### 4. オーバーレイ情報のカスタマイズ

```yaml
overlay_info: true
info_font_size: 20
info_color: [0, 255, 0]      # 緑色のテキスト
info_position: [50, 50]       # 右下の位置
```

## 注意事項

1. **ストレージ容量**: 長時間の学習では大量の動画ファイルが生成されます
2. **パフォーマンス**: 動画記録は学習速度に若干の影響を与えます
3. **メモリ使用量**: 動画記録用のメモリが追加で必要です
4. **ファイル管理**: 定期的に古い動画ファイルを削除することを推奨します

## サポート

動画記録機能に関する問題や質問がある場合は、以下のファイルを確認してください：

- `src/training/video_recorder.py`: 動画記録器の実装
- `src/training/parallel_trainer.py`: 学習プロセスとの統合
- `scripts/test_video_recording.py`: テストスクリプト
